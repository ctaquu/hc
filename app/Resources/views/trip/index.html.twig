{% extends 'base.html.twig' %}

{% block body %}
    <h2>Trips listing...</h2>.

    <table>
        <thead>
            <td>name</td>
            <td>file</td>
            <td>date</td>
        </thead>
    {% for trip in trips %}
            <tr>
                <td><a href="{{ path('trip_show', {'slug': trip.id}) }}">{{ trip.name }}</a></td>
                <td><a href="{{ asset('xml/trips/') ~ trip.imageName }}">{{ trip.imageName }}</a></td>
                {#<td><a href="{{ vich_uploader_asset(trip, 'image') }}">{{ trip.imageName }}</a></td>#}
                <td>some date...</td>
                {#<td>{{ trip.updatedAt }}</td>#}
            </tr>
        {#<ul>#}
            {#<li>trip->name:: {{ trip.name }}</li>#}
        {#</ul>#}
    {% endfor %}
    </table>

{% endblock %}
